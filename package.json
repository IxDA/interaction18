{
  "name": "interaction18",
  "version": "1.0.0",
  "description": "Interaction18.ixda.org website",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ixda/interaction18.git"
  },
  "devDependencies": {
    "autoprefixer": "^7.1.2",
    "onchange": "latest"
  },
  "scripts": {
    "autoprefixer": "postcss --use autoprefixer ./css/*.css -d build/",
    "build": "npm run autoprefixer",
    "build:watch": "onchange 'css/*.css' -- npm run build"
  },
  "dependencies": {
    "postcss-cli": "^4.1.0"
  }
}
